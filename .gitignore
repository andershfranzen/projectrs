node_modules/
dist/
**/.next/
*.lock
.vite/
*.db
*.db-shm
*.db-wal
server/data/maps/overworld.bak/
server/data/maps/overworld/standby.json
server/data/maps/*/backup.json
server/data/maps/*/backups/

# Local-only animation source files — too large for the repo
assets-sources/
pickaxeminingset_sourcefiles/

# Local MCP server config (points at user's home dir)
.mcp.json

# Local agent/review tool state and generated worktrees
.claude/
.clawpatch/

# Backup glbs kept locally
client/public/Character models/*backup*.glb

# Source/import assets that should not ship from client/public
client/public/assets/forced-textures/
client/public/assets/models/Bought packs/
client/public/assets/models/*.blend
client/public/assets/mobs/**/*.fbx
client/public/assets/bought-assets/sylora_bloomsacape_stylized_lowpoly_grass_pack/

# Editor assets served via proxy to server (symlink removed for Windows compat)
editor/public/assets
# Editor character GLB + animations served via proxy to client dev server
# (symlink removed for Windows compat — see editor/vite.config.js)
editor/public/Character models

# Runtime audit log + auto-generated gear-overrides backups (timestamped .bak
# files created by server/src/main.ts on each gear-overrides save).
server/data/audit.log
server/data/gear-overrides.*.bak
# NPC defs backups created by the /api/editor/npcs endpoint.
server/data/backups/
references/
assets-sources/

# GLB texture-optimizer backups — large local-only archives
tools/glb-backups-*.tar.gz

# Browser/CPU profiler captures
tools/profiler-runs/
